#include<iostream>
#include<algorithm>
#include<vector>
#include<random>
#include<ctime>
using namespace std;
int main()
{
	srand((unsigned)time(NULL));
	vector<int> tails;
	vector<int> nums;
	for (int i = 0; i < 5; i++)
	{
		nums.push_back(rand() % 100);
	}
	int m = nums.size();
	int start;
	int end;
	tails.push_back(nums[0]);
	for (int i = 1; i < m; i++)
	{
		auto it = upper_bound(tails.begin(), tails.end(), nums[i]);
		if (it == tails.end())//没找到比他大的数字
		{
			tails.push_back(nums[i]);
		}
		else
		{
			*it = nums[i];//迭代器解引用赋值
		}
	}
	for (int v : nums)
	{
		cout << v << " ";
	}
	cout << endl;
	cout << tails.size() << endl;
	return 0;
}
